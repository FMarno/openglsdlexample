# Simple-ish openGL example

A spinning cube with diffuse moving light.
Uses SDL, openGL with vertex and fragment shader.
lots of code copied and modified from all over.
I used VCPKG for the dependencies.